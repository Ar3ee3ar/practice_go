package calculator // divide function by package eg. db_func, cal_func etc.

func Add(number1, number2 int) int { // lowercase for local use } first letter uppercase can access by call package
	return number1 + number2
}
